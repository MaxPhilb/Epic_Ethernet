syntax = "proto3";

message DigitalOutput{
    int32 id =1;
    bool value=2;
}

message EpicEthernetOutput {

    repeated DigitalOutput digoutputs=1; [(nanopb).fixed.length= true ,(nanopb)max_size= = 32] //32 sorties
    
}

